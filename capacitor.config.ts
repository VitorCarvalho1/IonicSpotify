import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'AplicativoTeste',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
